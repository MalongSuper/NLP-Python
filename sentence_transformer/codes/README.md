### Run virtual environment:

```text
python3 -m venv venv
source venv/bin/activate
```

### Install required packages

```text
pip install -U sentence-transformers scikit-learn pandas numpy matplotlib
```


### Run the program

```text
python3 (paste the path to the file)
```